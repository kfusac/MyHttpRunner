bind_variables:
    variables:
        - TOKEN: "confcustom"
        - token: $TOKEN

builtin_functions:
    variables:
        - length: ${len(confcustom)}
        - smallest: ${min(2, 3, 8)}
        - largest: ${max(2, 3, 8)}